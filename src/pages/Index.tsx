import { ClusteringSimulation } from "@/components/simulation/ClusteringSimulation";

const Index = () => {
  return (
    <main className="min-h-screen bg-background">
      <ClusteringSimulation />
    </main>
  );
};

export default Index;
